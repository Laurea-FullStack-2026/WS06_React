# WS06 Week Overview

This week introduces React fundamentals on top of the existing REST API backend.

## Learning goals
- Build UI with components and JSX
- Manage data with props and state
- Fetch backend data with `useEffect`
- Implement forms and client-side routing
- Connect full CRUD flows to the existing `/api/posts` endpoints
